# ==============================================
# BACKEND ENVIRONMENT VARIABLES - EXAMPLE
# ==============================================
# Copy this file to .env.development and fill in your actual values
# NEVER commit .env.development to Git (it's in .gitignore)

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# Your local PostgreSQL connection details
DB_HOST=localhost
DB_PORT=5432
DB_NAME=incident_assistant
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=500
OPENAI_TEMPERATURE=0.7

# CORS Configuration
# Frontend URL that's allowed to access this API
CORS_ORIGIN=http://localhost:5173
