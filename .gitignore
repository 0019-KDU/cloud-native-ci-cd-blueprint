# Environment files with secrets
backend/.env
# Block staging and production secrets (contain real credentials)
infra/k8s/overlays/staging/*-secret.yaml
infra/k8s/overlays/production/*-secret.yaml
# Dev secrets are safe to commit (placeholder values only)

# Node
node_modules/

# Argo Rollouts kubectl plugin
kubectl-argo-rollouts.exe
