{
    "name": "e2e-tests",
    "version": "1.0.0",
    "description": "End-to-end tests for AI Incident Assistant",
    "scripts": {
        "test": "playwright test",
        "test:ui": "playwright test --ui",
        "test:headed": "playwright test --headed",
        "test:staging": "BASE_URL=http://129.212.208.28 playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.40.0"
    }
}