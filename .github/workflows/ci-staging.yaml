name: CI/CD - Staging Environment

on:
  # Manual trigger with inputs
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (leave empty for latest from dev)"
        required: false
        type: string
      skip_tests:
        description: "Skip E2E and Load tests"
        required: false
        type: boolean
        default: false
      run_load_test:
        description: "Run load tests"
        required: false
        type: boolean
        default: true
      run_e2e_test:
        description: "Run E2E tests"
        required: false
        type: boolean
        default: true

permissions:
  contents: write
  pull-requests: write
  actions: write

env:
  REGISTRY: registry.digitalocean.com
  IMAGE_NAME: ai-incident-assistant
  STAGING_NAMESPACE: staging
  ARGO_WORKFLOWS_URL: ${{ secrets.ARGO_WORKFLOWS_URL }}

jobs:
  # ============================================
  # JOB 1: Determine Image Tag
  # ============================================
  prepare:
    name: Prepare Deployment
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.get-tag.outputs.image_tag }}
      needs_update: ${{ steps.check-diff.outputs.needs_update }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get image tag
        id: get-tag
        run: |
          if [ -n "${{ inputs.image_tag }}" ]; then
            # Use provided tag
            echo "image_tag=${{ inputs.image_tag }}" >> $GITHUB_OUTPUT
            echo "📦 Using provided image tag: ${{ inputs.image_tag }}"
          else
            # Get current tag from dev environment
            DEV_TAG=$(grep "newTag:" infra/k8s/overlays/dev/kustomization.yaml | head -1 | sed 's/.*newTag:\s*//; s/"//g; s/^[[:space:]]*//; s/[[:space:]]*$//')
            echo "image_tag=$DEV_TAG" >> $GITHUB_OUTPUT
            echo "📦 Using dev image tag: $DEV_TAG"
          fi

      - name: Check if staging needs update
        id: check-diff
        run: |
          # Get current staging tag
          STAGING_TAG=$(grep "newTag:" infra/k8s/overlays/staging/kustomization.yaml | head -1 | sed 's/.*newTag:\s*//; s/"//g; s/^[[:space:]]*//; s/[[:space:]]*$//')
          TARGET_TAG="${{ steps.get-tag.outputs.image_tag }}"

          echo "Current staging tag: $STAGING_TAG"
          echo "Target tag: $TARGET_TAG"

          if [ "$STAGING_TAG" = "$TARGET_TAG" ]; then
            echo "needs_update=false" >> $GITHUB_OUTPUT
            echo "✅ Staging is already at $TARGET_TAG - will run tests only"
          else
            echo "needs_update=true" >> $GITHUB_OUTPUT
            echo "🔄 Staging needs update from $STAGING_TAG to $TARGET_TAG - will deploy + test"
          fi

  # ============================================
  # JOB 2: Deploy to Staging (Merge and Sync)
  # ============================================
  deploy-to-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [prepare]
    if: needs.prepare.outputs.needs_update == 'true'
    outputs:
      deployed: ${{ steps.deployment.outputs.deployed }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Update staging kustomization
        run: |
          cd infra/k8s/overlays/staging
          sed -i "s|newTag:.*|newTag: \"${{ needs.prepare.outputs.image_tag }}\"|g" kustomization.yaml

          echo "📝 Updated staging kustomization.yaml:"
          cat kustomization.yaml

      - name: Commit and push directly to main
        run: |
          git add infra/k8s/overlays/staging/kustomization.yaml
          git commit -m "🚀 Deploy to staging: ${{ needs.prepare.outputs.image_tag }}"
          git push origin main

          echo "✅ Pushed changes directly to main"

      - name: Install doctl
        run: |
          sudo snap install doctl
          sudo snap connect doctl:kube-config
          mkdir -p $HOME/.kube
          doctl auth init --access-token ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Save kubeconfig
        run: doctl kubernetes cluster kubeconfig save ai-incident-assistant

      - name: Trigger ArgoCD Sync
        run: |
          echo "🔄 Triggering ArgoCD sync for staging..."
          kubectl patch application ai-incident-staging -n argocd \
            --type merge \
            -p '{"operation": {"initiatedBy": {"username": "github-actions"}, "sync": {"revision": "HEAD"}}}' \
            || echo "⚠️ ArgoCD sync triggered"

      - name: Wait for ArgoCD sync
        run: |
          echo "⏳ Waiting for ArgoCD to sync..."
          for i in {1..30}; do
            SYNC_STATUS=$(kubectl get app ai-incident-staging -n argocd -o jsonpath='{.status.sync.status}' 2>/dev/null || echo "Unknown")
            HEALTH_STATUS=$(kubectl get app ai-incident-staging -n argocd -o jsonpath='{.status.health.status}' 2>/dev/null || echo "Unknown")
            
            echo "Attempt $i/30 - Sync: $SYNC_STATUS, Health: $HEALTH_STATUS"
            
            if [ "$SYNC_STATUS" = "Synced" ] && [ "$HEALTH_STATUS" = "Healthy" ]; then
              echo "✅ ArgoCD sync complete and healthy!"
              break
            fi
            
            sleep 10
          done

      - name: Wait for deployment rollout
        id: deployment
        run: |
          echo "⏳ Waiting for staging deployment to complete..."
          kubectl rollout status deployment/backend -n ${{ env.STAGING_NAMESPACE }} --timeout=5m
          kubectl rollout status deployment/frontend -n ${{ env.STAGING_NAMESPACE }} --timeout=5m

          echo "✅ Staging deployment complete!"
          echo "deployed=true" >> $GITHUB_OUTPUT

          echo ""
          echo "🌐 Staging URL: http://staging.174.138.120.13.nip.io"
          echo "📦 Image Tag: ${{ needs.prepare.outputs.image_tag }}"

  # ============================================
  # JOB 3: Run E2E Tests AFTER Deployment
  # ============================================
  e2e-tests:
    name: E2E Tests (Argo Workflow)
    runs-on: ubuntu-latest
    needs: [prepare, deploy-to-staging]
    if: ${{ inputs.run_e2e_test != false && needs.deploy-to-staging.outputs.deployed == 'true' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install doctl
        run: |
          sudo snap install doctl
          sudo snap connect doctl:kube-config
          mkdir -p $HOME/.kube
          doctl auth init --access-token ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Save kubeconfig
        run: doctl kubernetes cluster kubeconfig save ai-incident-assistant

      - name: Install Argo CLI
        run: |
          curl -sLO https://github.com/argoproj/argo-workflows/releases/download/v3.5.4/argo-linux-amd64.gz
          gunzip argo-linux-amd64.gz
          chmod +x argo-linux-amd64
          sudo mv argo-linux-amd64 /usr/local/bin/argo

      - name: Submit E2E Test Workflow
        id: submit-e2e
        run: |
          WORKFLOW_NAME=$(argo submit infra/argo-workflows/e2e-tests.yaml \
            -n argo \
            --parameter image_tag=${{ needs.prepare.outputs.image_tag }} \
            --parameter staging_url=http://staging.174.138.120.13.nip.io \
            -o name)

          echo "workflow_name=$WORKFLOW_NAME" >> $GITHUB_OUTPUT
          echo "🧪 Submitted E2E workflow: $WORKFLOW_NAME"

      - name: Wait for E2E Tests
        run: |
          echo "⏳ Waiting for E2E tests to complete..."
          argo wait ${{ steps.submit-e2e.outputs.workflow_name }} -n argo

      - name: Get E2E Results
        if: always()
        run: |
          argo get ${{ steps.submit-e2e.outputs.workflow_name }} -n argo

          # Check status
          STATUS=$(argo get ${{ steps.submit-e2e.outputs.workflow_name }} -n argo -o json | jq -r '.status.phase')
          if [ "$STATUS" != "Succeeded" ]; then
            echo "❌ E2E tests failed!"
            argo logs ${{ steps.submit-e2e.outputs.workflow_name }} -n argo
            exit 1
          fi

          echo "✅ E2E tests passed!"

  # ============================================
  # JOB 4: Run Load Tests AFTER E2E Tests
  # ============================================
  load-tests:
    name: Load Tests (Argo Workflow)
    runs-on: ubuntu-latest
    needs: [prepare, deploy-to-staging, e2e-tests]
    if: ${{ inputs.run_load_test != false && needs.e2e-tests.result == 'success' && needs.deploy-to-staging.outputs.deployed == 'true' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install doctl
        run: |
          sudo snap install doctl
          sudo snap connect doctl:kube-config
          mkdir -p $HOME/.kube
          doctl auth init --access-token ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Save kubeconfig
        run: doctl kubernetes cluster kubeconfig save ai-incident-assistant

      - name: Install Argo CLI
        run: |
          curl -sLO https://github.com/argoproj/argo-workflows/releases/download/v3.5.4/argo-linux-amd64.gz
          gunzip argo-linux-amd64.gz
          chmod +x argo-linux-amd64
          sudo mv argo-linux-amd64 /usr/local/bin/argo

      - name: Submit Load Test Workflow
        id: submit-load
        run: |
          WORKFLOW_NAME=$(argo submit infra/argo-workflows/load-tests.yaml \
            -n argo \
            --parameter image_tag=${{ needs.prepare.outputs.image_tag }} \
            --parameter staging_url=http://staging.174.138.120.13.nip.io \
            --parameter duration=5m \
            --parameter users=50 \
            -o name)

          echo "workflow_name=$WORKFLOW_NAME" >> $GITHUB_OUTPUT
          echo "📊 Submitted Load Test workflow: $WORKFLOW_NAME"

      - name: Wait for Load Tests
        run: |
          echo "⏳ Waiting for load tests to complete..."
          argo wait ${{ steps.submit-load.outputs.workflow_name }} -n argo

      - name: Get Load Test Results
        if: always()
        run: |
          argo get ${{ steps.submit-load.outputs.workflow_name }} -n argo

          STATUS=$(argo get ${{ steps.submit-load.outputs.workflow_name }} -n argo -o json | jq -r '.status.phase')
          if [ "$STATUS" != "Succeeded" ]; then
            echo "❌ Load tests failed!"
            argo logs ${{ steps.submit-load.outputs.workflow_name }} -n argo
            exit 1
          fi

          echo "✅ Load tests passed!"

  # ============================================
  # JOB 5: Notify Results
  # ============================================
  notify:
    name: Notify Results
    runs-on: ubuntu-latest
    needs: [prepare, deploy-to-staging, e2e-tests, load-tests]
    if: always()

    steps:
      - name: Send Slack - Success
        if: ${{ needs.deploy-to-staging.outputs.deployed == 'true' && needs.e2e-tests.result == 'success' && (needs.load-tests.result == 'success' || needs.load-tests.result == 'skipped') }}
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": "✅ Staging deployment successful!",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "✅ Staging Deployment & Tests Complete"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Image Tag:*\n`${{ needs.prepare.outputs.image_tag }}`"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Triggered by:*\n${{ github.actor }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Deployment:*\n✅ Successful"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*E2E Tests:*\n${{ needs.e2e-tests.result == 'success' && '✅ Passed' || '⏭️ Skipped' }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Load Tests:*\n${{ needs.load-tests.result == 'success' && '✅ Passed' || '⏭️ Skipped' }}"
                    }
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "🎭 <http://staging.174.138.120.13.nip.io|View Staging Environment>\n\n*Ready for production deployment!*"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK

      - name: Send Slack - Failure
        if: ${{ needs.deploy-to-staging.result == 'failure' || needs.e2e-tests.result == 'failure' || needs.load-tests.result == 'failure' }}
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": "❌ Staging deployment failed!",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "❌ Staging Deployment or Tests Failed"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Image Tag:*\n`${{ needs.prepare.outputs.image_tag }}`"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Deployment:*\n${{ needs.deploy-to-staging.result }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*E2E Tests:*\n${{ needs.e2e-tests.result }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Load Tests:*\n${{ needs.load-tests.result }}"
                    }
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|🔗 View Failed Run>"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
